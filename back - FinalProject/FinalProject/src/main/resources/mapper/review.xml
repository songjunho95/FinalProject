<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.ReviewMapper">
	
	<insert id="create" parameterType="Review"  useGeneratedKeys="true"  keyProperty="no" keyColumn="no" >
		INSERT INTO review(review_rating, review_opinion, review_img)
		VALUES (#{review_rating}, #{review_opinion}, #{review_img})
	</insert>
	
	<select id="selectAll" parameterType="Paging" resultType="Review">
		SELECT * FROM review
		LIMIT #{offset}, #{limit}
	</select>
	
	
	
	
	<insert id="update" parameterType="Review">
		INSERT INTO review(review_rating, review_opinion, review_img)
		VALUES (#{review_rating}, #{review_opinion}, #{review_img})
	</insert>
	


</mapper>